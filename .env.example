# Neon Postgres connection string used by api/contact.ts
# Get this from the Neon dashboard or `vercel env pull` after linking the
# Neon integration on Vercel. Use the pooled connection string.
DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
